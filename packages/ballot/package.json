{
  "name": "@vocdoni/ballot",
  "version": "1.2.0",
  "license": "GPL-3.0-or-later",
  "description": "Framework-agnostic Vocdoni ballot semantics: type inference, choice encoding, results decoding",
  "keywords": [
    "vocdoni",
    "ballot",
    "election",
    "voting"
  ],
  "homepage": "https://github.com/vocdoni/vocdoni-integrator-sdk",
  "bugs": "https://github.com/vocdoni/vocdoni-integrator-sdk/issues",
  "repository": {
    "type": "git",
    "url": "https://github.com/vocdoni/vocdoni-integrator-sdk.git",
    "directory": "packages/ballot"
  },
  "type": "module",
  "files": [
    "dist"
  ],
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist .turbo",
    "prepack": "clean-package",
    "postpack": "mv -fv package.json.backup package.json"
  },
  "dependencies": {
    "@vocdoni/api-types": "workspace:*"
  },
  "devDependencies": {
    "tsconfig": "workspace:*",
    "tsdown": "^0.22.3",
    "typescript": "^5.7.0"
  },
  "clean-package": "../../clean-package.config.json"
}
